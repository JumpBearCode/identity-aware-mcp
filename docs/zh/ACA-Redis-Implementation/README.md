# ACA → Redis:Sandbox 执行层的设计与落地(文档索引)

这个文件夹收录 **Identity-Aware MCP 的 sandbox 执行层**从"把沙箱搬到 ACA"到"用 Redis 做水平扩展协调(分布式锁 + Reaper 选主)"的整条**设计与落地脉络**。对应代码在 `fix-redis` 分支(功能提交 `737e437`;每块代码对应哪个 commit、上没上、开没开,见 **#7** 的 commit 矩阵)。

**按下表顺序读**即这条线的时间顺序,也是逻辑顺序:起点(为什么 / 怎么迁到 ACA)→ 设计原理 → 选型认知 → 落地方案 → 实测标定 → 收口总览。只想看**最终形态**就直接跳 **#7**。

| # | 阶段 | 文档 | 是什么 | 首次提交 |
|---|---|---|---|---|
| 1 | 起点 | [ACA-Sandbox-迁移方案.md](ACA-Sandbox-迁移方案.md) | 把 per-user sandbox 迁到 Azure Container Apps(Session 级粘性 + server 无状态)的迁移方案:术语、粒度、exec 模型。 | 06-28 · `267e5b9` |
| 2 | 设计 · 原理 | [MCP-水平扩展-分布式锁与Reaper选主.md](MCP-水平扩展-分布式锁与Reaper选主.md) | 多副本水平扩展的**原理篇**:进程内状态还差什么、那把锁到底锁什么、分布式锁(`SET NX` / TTL / token / Lua 释放)、Reaper 选主怎么写。 | 06-29 · `508efef` |
| 3 | 设计 · 原理 | [Sandbox-生命周期.md](Sandbox-生命周期.md) | 一台 sandbox 从出生到死亡:谁触发创建、bootstrap 做了什么、多次 tool call 间怎么保活、空闲挂起、最终谁回收。 | 06-29 · `508efef` |
| 4 | 设计 · 原理 | [分布式原语-数据库-编排三者对照.md](分布式原语-数据库-编排三者对照.md) | 认知对照:分布式原语 / 数据库 / 编排器其实是同一套并发控制的三个"展厅";为什么这里落到 Redis。 | 07-04 · `0b0706d` |
| 5 | 落地 | [MCP-分布式锁与Reaper选主-实现方案.md](MCP-分布式锁与Reaper选主-实现方案.md) | **落地方案**(大白话 + 客服坐席比喻):把原理翻成具体 PR —— 两层锁、create 超时、失败回滚、Reaper 选主,并列出实际改了哪些代码。 | 07-04 · `d9eb49e` |
| 6 | 实测 | [MCP-Sandbox-创建耗时实测报告.md](MCP-Sandbox-创建耗时实测报告.md) | **实测**:sandbox 创建各段(A 镜像 / B volume / C microVM / D auto-delete / E bootstrap)耗时,用来标定 `create_timeout` / lock TTL。 | 07-04 · `d9eb49e` |
| 7 | 收口 | [MCP-实现总览-时序图与commit状态.md](MCP-实现总览-时序图与commit状态.md) | **收口总览**:最终实现的时序图 + Redis 数据模型 + 时间参数,每块标 commit hash 与开关状态,含 Q&A 补充与端到端测试。 | 07-04 · `0e286b0` |

> 排序依据:每个文档**首次被创建**的 commit 时间(跟着文件夹移动回溯)。#2 与 #3、#5 与 #6 各是同一次提交(`508efef` / `d9eb49e`)同批产出,内部不分先后;表内按逻辑阅读顺序排。所有文件"最后一次提交"时间相同,是因为它们被同一个 commit 一起挪进本文件夹,故不能用来排序。

**一句话脉络:** 迁到 ACA(#1)→ 想清楚多副本要什么(#2–#4)→ 写成方案(#5)→ 实测标定(#6)→ 收口成带 commit 状态的总览(#7)。
